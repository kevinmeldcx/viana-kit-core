{
  "name": "viana-kit-core",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "sync": "node scripts/sync.js",
    "sync:target": "node scripts/sync.js --target"
  },
  "packageManager": "npm@11.5.2",
  "devDependencies": {
    "turbo": "^2.8.20"
  }
}
